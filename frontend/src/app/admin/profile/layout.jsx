// frontend/src/app/admin/profile/layout.jsx
export const metadata = {
  title: "Profil Admin | Demo Showroom",
  description: "Halaman admin untuk melihat dan mengedit profil pengguna.",
};

export default function ProfileLayout({ children }) {
  return <div>{children}</div>;
}
